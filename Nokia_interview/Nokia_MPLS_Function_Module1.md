# 🧩 模块 1：函数与作用域（Functions & Scope）

## 🎯 学习目标

-   理解函数定义与调用机制；
-   掌握参数传递方式（位置 / 关键字 / 可变参数）；
-   理解返回值与作用域；
-   熟悉异常处理；
-   能写出稳定的小测试函数。

------------------------------------------------------------------------

## 一、函数基础定义与调用

``` python
def ping_device(ip, count=3):
    """Ping a device multiple times and return success count"""
    import subprocess
    success = 0
    for i in range(count):
        result = subprocess.run(["ping", "-c", "1", ip],
                                capture_output=True)
        if result.returncode == 0:
            success += 1
    return success
```

✅ **知识点讲解** - `def`：定义函数 - `ip, count`：形参（parameters） -
`count=3`：默认参数 - `return`：返回值 -
`import subprocess`：在函数内导入库（仅作用域内可用）

Do you know the difference between concurrency and parallelism?

------------------------------------------------------------------------

## 二、参数类型详解

``` python
def demo(a, b=2, *args, **kwargs):
    print(a, b)
    print(args)
    print(kwargs)

demo(1, 3, 5, 6, x=10, y=20)
```

输出：

    1 3
    (5, 6)
    {'x': 10, 'y': 20}

✅ **知识重点** - 位置参数、默认参数、可变参数、关键字参数。

------------------------------------------------------------------------

## 三、作用域与变量（Scope）

``` python
x = 10

def show():
    x = 5
    print("Inside:", x)

show()
print("Outside:", x)
```

-   局部变量(local)：函数内部定义。
-   全局变量(global)：函数外定义。
-   使用 `global` 关键字可在函数中修改全局变量。

------------------------------------------------------------------------

## 四、Lambda（匿名函数）

``` python
add = lambda a, b: a + b
print(add(3, 5))
```

用途：快速定义小函数；常配合 `map()`、`filter()`、`sorted()`。

------------------------------------------------------------------------

## 五、异常处理（try / except）

``` python
def safe_divide(a, b):
    try:
        return a / b
    except ZeroDivisionError:
        print("❌ Cannot divide by zero")
        return None
```

-   精确捕获异常类型，不要用裸 `except:`
-   面试重点：如何处理 ping 失败、文件缺失等错误。

------------------------------------------------------------------------

## 六、练习题

### 🧠 练习题 1（中等）

**题目**：写一个函数 `analyze_logs(file_path)`\
统计日志文件中每台设备的 UP 次数，返回字典结果。

### 🧠 练习题 2（稍难）

**题目**：写一个函数 `ping_many(devices)`\
依次 ping 每个设备，返回如：

``` python
{'10.0.0.1': True, '10.0.0.2': False}
```

------------------------------------------------------------------------

## ✅ 小结复盘

  小节         关键能力          面试官测试什么
  ------------ ----------------- ----------------
  函数定义     结构与逻辑清晰    代码组织能力
  参数传递     默认 / 可变参数   代码通用性
  作用域       变量影响范围      理解执行流程
  Lambda       快速表达          Python 熟练度
  try/except   稳定性            自动化健壮性
